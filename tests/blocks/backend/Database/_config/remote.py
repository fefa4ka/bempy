class Modificator:
    """
        A remote database implementation
    """

    def init(self, host='127.0.0.1'):
        """
            host -- server's port
        """

        print(self.name + ': Remote Database server connection on host =', host)
